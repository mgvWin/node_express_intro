const path = require("path");

module.exports = {
  config: path.resolve("migrate_tmp", "configs", "sequelize.js"),
  "models-path": path.resolve("migrate_tmp", "db", "models"),
  "seeders-path": path.resolve("migrate_tmp", "db", "seeders"),
  "migrations-path": path.resolve("migrate_tmp", "db", "migrations")
};